<h1><%= t('.title') %></h1>
<div class="container pt-3">
  <!-- 検索フォーム -->
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <form>
        <div class="input-group mb-3"><input class="form-control" placeholder="検索ワード" type="search"/>
          <div class="input-group-append"><input type="submit" value="検索" class="btn btn-primary"/></div>
        </div>
      </form>
        <%# <%= render 'search', q: @q, url: boards_path %> <%# ゆくゆくはこっちを使うかも%>
    </div>
  </div>
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @items.present? %>
    		  <%= render @items %>
		    <% else %>
		      <p><%= t('defaults.flash_message.no_item') %></p>
		    <% end %>
        <%# <%= paginate @items, theme: 'bootstrap-5' %> 
      </div>
    </div>
  </div>
</div>
